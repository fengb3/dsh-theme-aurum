# PROTOTYPE-SPEC.md — DSH 主题原型设计契约(设计师交付版)

你拿到这份文件,说明要为一个 DSH(DeepSeek Harness)Web 界面做**主题原型**。
本文件自包含:不需要读仓库其他文档即可开工。仓库内部流程以 AGENTS.md 为准,
但那些是实现侧的事,与你无关。

**一句话分工:你交一份单文件 HTML 原型;实现侧把你的 CSS 整段拷进主题插件、
HTML 片段贴进 React 组件,再用双页几何 diff 门禁逐 selector 比对(±1.5px)。**

因此:

- 你的 CSS 类名 = 线上类名;你的变量 = 线上变量;你的动画数值 = 线上数值;
- **风格完全自由**(配色/字体/圆角/密度/装饰语言随你定),**结构契约必须遵守**
  (锚点写法/双主题/状态钩子/类名纪律/selector 稳定);
- 原型规整度直接决定移植成本:守约 → 贴进来跑门禁即完成;违约 → 残差或返工。

---

## 0 · 你的产出如何被消费(恒等映射流水)

实现侧有一条「消灭双重翻译」的流水,它对原形的期待是**机械可拷贝**:

| 流水步骤 | 对原型的要求 |
|---|---|
| CSS 整段拷贝,仅两个机械替换:`:root` → `body[data-ds-dark-theme]`;`html[data-theme="light"]` → `body:not([data-ds-dark-theme])` | 深/浅主题锚点**必须**写成这两种(§3) |
| HTML 片段贴进 htm 模板,静态文本换 `${}` 绑定、补 `onClick` | 结构语义清晰、类名即样式钩子(§2/§5) |
| 同 selector 双页 rect/字号 diff,±1.5px 自动断言 | **selector 在迭代中保持稳定**(§5,门禁的命根) |

设计任何形态前先自问一句:「这个视觉能不能用纯 CSS + 类名/data 钩子表达?」
能 → 放心画;不能 → 看 §10(落不了地的形态)或打 `PORT-NOTE:`(§9)。

## 1 · 交付物

### 1.1 主文件

- 路径:`prototype/<主题名>-workspace.html`,**单文件自包含**;
- 根元素 `<html lang="zh-CN" data-theme="dark">`(深色默认);
- 样式全部内联 `<style>`,按 §2 的十个分区分节,节注释格式
  `============ N · 标题 ============`(N 与 §2 表对应);
- 字体:Google Fonts `<link>` 直链可用(实现侧会同步拷 @import);
  展示/衬线族若无中文字重,**回退链必须落 CJK 字体**,否则中文样本失真;
- 脚本:JS 只允许做演示态切换(主题切换/示例数据注入),**不作为移植依据**,
  一切视觉必须 CSS 达成;
- `file://` 直开可用(门禁以本地文件方式加载原型页);
- 内容确定性:禁随机数/时间戳,否则双页 diff 产生噪声。

### 1.2 文件头元信息块(必须)

```html
<!--
THEME:     <主题名>(中文 + 拼音/英文短名)
PREFIX:    <2–3 字母短前缀>(keyframes 用,见 §7)
STYLE:     <风格关键词,一两个词>
SECTIONS:  1–10(§8 跳过则注明)
PORT-NOTE: <n> 处(grep PORT-NOTE 可盘点)
SELECTORS: 见文件尾附录 A
-->
```

### 1.3 附录 A:selector 清单(必须,见 §12 模板)

门禁拿它逐条比对原型页与实况页。**每条 = 一个会被几何断言的元素**。

## 2 · 骨架:必须覆盖的十个分区

DSH 工作区的完整画面地图。每区都要有**深浅两份**;列表类给 3–6 行真实感
中文样本;空态/加载态/错误态各一帧;交互件 hover / `:focus-visible` /
disabled 三态都要有样式帧。

| § | 分区 | 内容要点 | 必含状态帧 |
|---|---|---|---|
| 1 | 令牌 + 画布 | 双主题变量组(§3);底色/纹理(点阵/网格/纯净均可)/滚动条/文字 selection/focus ring | 深 × 浅 |
| 2 | 基础 | 四族字体/重置/通用基元:btn、icon-btn、pill、input | hover/disabled/focus |
| 3 | 应用骨架 | 三栏 grid(侧栏 264 / 主区 / 详情列——详情列可为 0)+ 浮层层级约定 | — |
| 4 | 侧栏 | 品牌行(字标+logo)/新建钮/目录头(分组折叠)/会话行(状态槽+悬停操作)/展开式搜索/视图切换/添加工作区行/底部设置+主题钮;**折叠细条 56px** 形态单独一帧 | 分组开合、行 hover、运行/等待/完成三态槽、行内重命名态、拖拽落点金线 |
| 5 | 会话流 | 用户气泡(文本+图片)/assistant 正文(md:标题/列表/行内码/代码块/表格)/推理卡(think)/上下文注入卡/尾部节点(操作+统计)/自动重试行/错误行/上限行 | 推理运行中 vs 完成、卡片展开 vs 收合、错误/成功胶囊 |
| 6 | 输入坞 | todo 进度条+胶囊/goal 条/queue 条(输入卡上方)/输入卡/命令钮/发送钮/mode+model 钮/上下文圆环+构成面板/统计行 | 空/有值/发送禁用/回合运行中 |
| 7 | 工具卡 | 卡壳(图标瓦片/工具名/参数摘要/状态胶囊/chevron)+ 展开体;已知工具特判(搜索/读取/编辑/清单/联网/终端/diff 各一帧)+ **未知工具兜底卡**;子调用缩进列表 | 运行(辉光扫掠)/完成/失败/展开/收合 |
| 8 | 瀑布图(可选) | 轨迹可视化;默认可整节跳过(注明即可),跳过属实现侧残差 | — |
| 9 | 全局浮层 | hero 空态(大标题/badge/工作区芯片/辉光)/通用菜单(分组头/快捷键/danger 项)/设置弹窗(mask+左导航双栏+内容行+关闭叉)/toast/scrim | 菜单项 hover、选中项、弹窗开 |
| 10 | 响应式 | 断点 820/640/480 逐档降密度;≤820 移动形态 = 48px 顶栏 + 左侧抽屉 + 遮罩 | 每档至少一帧 |

样本数据要求:会话标题/工具参数/正文片段用真实感中文,长度要覆盖「截断
省略(ellipsis)」与「多行换行」两种形态——门禁量的是几何,样本长度就是
几何的一部分。

## 3 · 设计令牌:结构固定,值自由

**一切颜色/字体/阴影/圆角/关键几何走 CSS 变量,禁散写字面量**(一次性
布局结构值除外)。深色 = `:root` 默认;浅色 = `html[data-theme="light"]`
覆盖块。这两个锚点是流水机械替换的**唯一目标**——写成任何别的形式
(`body[data-mode]`、`.dark`、`[data-theme]` 单属性等)流水直接断。

必备 token 族(键名风格随主题气质,但**族要齐**、kebab-case、族内后缀一致;
aurum 填值示例见附录 B):

| 族 | 键(示例名) | 说明 |
|---|---|---|
| 画布 | `--bg` `--bg-deep` + 纹理键(`--dot` 等) | 页底与最深层 |
| 表面 | `--surface` `--surface-2`(+`-3` 按需) | 卡片/面板分层 |
| 栏面 | `--rail-1` `--rail-2` `--rail-raised` | 侧栏/栏条渐变面 |
| 文字 | `--fg` `--muted` `--faint` | 三级对比 |
| 描边 | `--border` `--border-soft` | 推荐恒 transparent,见 §6 |
| 主强调 | `××` `××-strong` `××-dim` `××-ink` | 主色四件套:常规/强调/弱化/反白文字 |
| 次强调 | `--rose` 一族或自定 | 渐变搭档/警戒点缀 |
| 状态 | `--success` `--danger`(+`--warn` 按需) | 成功/危险/警告 |
| 阴影 | `--shadow-panel`(+栏影按需) | 浮层/卡片影 |
| 字体 | `--font-display` `--font-serif` `--font-ui` `--font-mono` | 四族;展示/衬线须含 CJK 回退 |
| 几何 | 栏宽/头高/关键圆角等 | 提 token 并**注释推导关系**(例:`--rail-inset` 12px 与 `--rail-head` 58px 撑起头部中心线同轴) |

## 4 · 双主题规则

- 每条新样式深浅两份都要过——只画深色 = 交付未完成;
- 浅色**不是深色镜像**:纹理换墨色(略调透明度保可读)、强纹理效果(辉光/
  sheen)减弱或关、实底主色钮上的文字用 `-ink` 反白键;
- 原型必须自带主题切换演示(点得到、看得见);
- 浅色特例逐条写在 `html[data-theme="light"] .selector{}` 覆盖块里,
  不要另起一套类名。

## 5 · 类名与状态钩子(selector 稳定 = 门禁的命根)

- 类名语义化、可读——**原型类名会原样成为线上类名**;
- 禁模仿官方混淆前缀风格(`hHd-Xa_`/`wSkVaW_` 等短随机段);原型是独立
  页面,不得引用或假设任何官方 DOM 类名/结构;
- 主题短前缀只用于 keyframes 与防全局冲突的极端场景,组件类名保持无前缀
  语义名(如 `.tool` `.reasoning` `.sidebar`);
- 状态一律用类名或 data 属性钩子:`.open` `.on` `.done` `.now` `.dragging`
  `[data-state=running]`——这些钩子逐字进实现侧 CSS 与组件;
- 状态修饰类**挂基础类之上**(`.todo-it.done`),不另起平行整套;
- **selector 稳定是硬约束**:门禁按 selector 双页比对,你中途改类名/改
  嵌套结构,门禁立刻失明或误报。迭代改版时:类名与结构变更 = **破坏性
  变更**,必须 `PORT-NOTE: BREAKING` 声明并同步附录 A(§9);
- 落地前自查与常见全局类冲突(`.menu` `.container` 这类泛名易撞),撞了
  就加主题词修饰(`.au-menu` 式),但这是例外不是惯例。

## 6 · 视觉构造规则

- **卡片即容器**:圆角/阴影/`overflow:hidden` 落在承载内容的元素上。
  禁止 `::before` 画一张卡再让内容铺到卡外——内容溢出圆角是历史实录;
- **无描边是推荐默认**:分隔靠面色 tint / 阴影 / 透明度层次。若你的风格
  确需可见描边:可以,但**每一处都要 `PORT-NOTE:` 标注**——实现侧主题
  token 压不过官方边框变量,需逐元素直写,成本显著高于面色分层;
- 伪元素只做装饰(角标/加号/扫光/状态点),注意与实体元素叠加的双渲染
  风险;子串选择器别用(`[class*=…]` 在实现侧有命中扩散史);
- 浮层用 `position:fixed` + 明确 z-index 层级表(注释声明各层量级);
- 图标:内联 SVG 随意画;实现侧会把常用图标映射到官方图标组件,
  形态特异的会被保留——**别把视觉重心押在图标独特性上**。

## 7 · 动画与 keyframes

- keyframes 名全局唯一,统一**主题短前缀**(文件头 PREFIX 声明,如 aurum
  用 `au-`)——防与官方及其他插件冲突(历史实录:裸名 `pulse` 撞车);
- 每个动画配 `@media (prefers-reduced-motion:reduce)` 豁免;
- **数值即移植值**:时长/缓动/延迟写实数——门禁会逐字比对 computed
  值(历史实录:展开曲线三元组逐字比对断言);
- 入场动画**慎用 translateY**(会把刚滚动到底的视口顶起),优先
  opacity / filter 形态;
- 展开/收合推荐 `grid-template-rows: 0fr ⇄ 1fr` 插值模式(实现侧成熟
  先例,内容层配 opacity/transform 淡入,曲线双档:收合快、展开慢)。

## 8 · 响应式与文件完整性

- 断点档位固定 **820 / 640 / 480**(与实现侧对齐);要加档必须
  `PORT-NOTE:` 声明;
- ≤820 移动形态:48px 顶栏(横排钮组)+ 左侧抽屉(完整侧栏内容滑入)+
  遮罩——结构由实现侧组件接管,原型给样式基准 + `PORT-NOTE:`(§9);
- **所有 `@media`/`@keyframes`/规则花括号必须配平**——实现侧 CSS 是字符串
  数组拼接、无语法检查,漏一个 `}` 会静默吞掉后续所有规则(历史实录:
  媒体块一路吞到文件尾,桌面端样式全灭);
- CSS 原生嵌套(`&`)可用,保持一层浅嵌套。

## 9 · PORT-NOTE 标注约定

移植相关的非常规决策,统一 `PORT-NOTE:` 前缀(HTML 用
`<!-- PORT-NOTE: … -->`,CSS 用 `/* PORT-NOTE: … */`),保证可 grep、可盘点。
**强制标注五类**:

1. **交互密集区**(拖拽排序/浮动菜单/行内改名状态机)→ 标
   「结构照写组件,原型仅样式基准」;
2. **可见描边偏离**(逐处,见 §6);
3. **CSS 不可达状态**(依赖运行时数据,如圆环占用率)→ 标
   「不适用 + 原因」;
4. **破坏性变更**(类名/结构/断点改动)→ 标 `PORT-NOTE: BREAKING` 并同步附录 A;
5. **跳过的分区**(如 §8 瀑布图)→ 标「整节跳过,记实现侧残差」。

## 10 · 不要设计这些(落不了地)

1. **官方 DOM 手术类**:重排官方结构、替换官方 SVG 内容——React 重渲染
   会抹掉,DOM 补丁是死路;
2. **CSS 读不到的状态**:React 内部 state、canvas 内部量、视口外环境量;
3. **定时轮询/JS 补丁类视觉**;
4. **依赖官方混淆类名的选择器**(它们随版本漂移,原型根本不该出现)。

视觉上必须有而原型表达不了的 → 设计成等价可达形态(如 `[data-state]`
钩子挂状态),或 `PORT-NOTE:` 记残差交实现侧决策——**不悄悄画进去**。

## 11 · 交付前自检 + 验收流程

**自检清单(交付前逐项过)**:

- [ ] `file://` 直开正常;主题切换演示可用;深浅两态目检全部分区;
- [ ] 锚点写法 = `:root` / `html[data-theme="light"]`,别无分号;
- [ ] 花括号配平(`{` 与 `}` 计数相等;每个 `@media` 收尾肉眼复核);
- [ ] keyframes 全带主题前缀;动画全带 reduced-motion 豁免;
- [ ] PORT-NOTE 五类该标的都标了;文件头计数与实际一致;
- [ ] 附录 A 与当前 DOM 实际 selector 一致(改过结构的话);
- [ ] 无随机数/时间戳;列表样本 3–6 行且覆盖截断态。

**验收(实现侧执行,你配合返修)**:

- 实现侧把附录 A 填进 `verify/verify-proto-diff.js` 的 SELECTORS →
  `node verify-run.mjs verify/verify-proto-diff.js`,±1.5px 全绿;
- 颜色并排人工复核;几何返修**只改被点名的差异**,不要整页翻新
  (翻新 = 破坏性变更,见 §9)。

## 12 · 附录 A 模板(selector 清单)

```html
<!-- APPENDIX-A SELECTORS v1(门禁比对清单;改动须 PORT-NOTE: BREAKING)
canvas.dot-grid      body
sidebar.card         .sidebar
sidebar.rail         .app.no-sb .sidebar
flow.tool            .tool
flow.reasoning       .reasoning
flow.turn-tail       .turn-tail
composer.card        .composer
overlay.menu         .menu
(每行:门禁标签 + selector;标签稳定,selector 变更走 BREAKING)
-->
```

## 13 · 附录 B:aurum 填值示例(参考,不是要求)

| 族 | aurum 深色值(节选) |
|---|---|
| 画布 | `--bg: oklch(16% .014 330)` `--dot: oklch(83% .115 88 / .08)` |
| 表面 | `--surface: oklch(21% .016 328)` `--surface-2: oklch(25.5% .018 326)` |
| 文字 | `--fg: oklch(93% .015 85)` `--muted` `--faint` 三级递减 |
| 描边 | `--border: transparent`(全族透明,无描边原则) |
| 主强调 | `--gold: oklch(83% .115 88)` + `-strong/-dim/-ink` 四件套 |
| 字体 | Cormorant Garamond / Noto Serif SC / Noto Sans SC / JetBrains Mono |

——风格自由,契约固定。把这份文件当成「画布边框」而不是「配色卡」:
边框之内,随便你挥洒。
